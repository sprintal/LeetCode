/**
 * @param {number} x
 * @return {number}
 */
var mySqrt = function(x) {
    return Math.floor(Math.sqrt(x));
};

// Runtime: 68 ms, faster than 92.12% of JavaScript online submissions for Sqrt(x).
// Memory Usage: 35.5 MB, less than 80.55% of JavaScript online submissions for Sqrt(x).